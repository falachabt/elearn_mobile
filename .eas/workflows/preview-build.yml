name: Preview Build

on:
  push:
    tags:
      - "v*-rc*"
      - "v*-preview*"

jobs:
  build:
    name: Build Android Preview
    type: build
    params:
      platform: android
      profile: preview
