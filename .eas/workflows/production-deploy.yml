name: Production Build & Submit

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build Android
    type: build
    params:
      platform: android
      profile: production

  submit:
    name: Submit to Google Play
    type: submit
    needs: [build]
    params:
      build_id: ${{ needs.build.outputs.build_id }}
